class Student():
	def __init__(self, name = "Student, Sample", period = 0, github = 'username')
		self.name = name
		self.period = period
		self.github = github
		self.assignments = []
		self.submissions = 0
	
	def __str__(self):
		rep = f"{self.period}: {self.name}\n"
		rep += f"{self.class} - {self.github}\n"
		rep += f"{Submissions: {self.submissions}"
		return rep

	@property
	def class(self):
		if self.period in classes['1400']:
			return "Java"
		elif self.period in in Student.classes['1030']:
			return "Python"
	
	def add_assignments(self):
		for i in 
		self.assignments.append(assign)

	

		
